//Itera em todos os atributos de um objeto
for (const indice in object) {
    if (Object.hasOwnProperty.call(object, indice)) {
        const element = object[indice];

    }
}


for (const valor of object) {

}

array.forEach(element => {

});

array.forEach(function (valor, indice, arrayCompleto) {

});